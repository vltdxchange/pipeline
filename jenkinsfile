pipeline
{
    agent any

    stages
    {
        stage("Build Image")
        {
            steps
            {
                  docker build -t myimage .
                  docker run -t -d myimage
                  def container=docker ps
            }
        
        }
      
    }
}