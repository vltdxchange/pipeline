pipeline
{
    def container;
    agent 
    {
        docker build -t myimage .
        docker run -t -d myimage
        container=docker ps
    }

    stages
    {
        stage("Build Image")
        {
            steps
            {
                  echo 'hello'
            }
        
        }
      
    }
}